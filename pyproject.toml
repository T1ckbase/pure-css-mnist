[project]
name = "pure-css-mnist"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.0",
    "tinygrad>=0.13.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
